const ComponentTypes = {
  FUNCTION: 'FUNCTION',
  CLASS: 'CLASS',
  VALUE: 'VALUE'
}

const ContainerElementTypes = {
  COMPONENT: 'COMPONENT',
  GROUP: 'GROUP'
}

const Lifetime = {
  TRANSIENT: 'TRANSIENT',
  SINGLETON: 'SINGLETON'
}

module.exports = {
  ContainerElementTypes,
  ComponentTypes,
  Lifetime
}
